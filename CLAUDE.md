# CLAUDE.md

**Before anything else in a session: read `HANDOFF.md`, then `README.md`.**

This repo is worked by two agents, Grok and Claude. `HANDOFF.md` is the shared log and it
owns the rules — read them there, in full, every session. The short version:

- Append your note **newest-first** under `## Log`, in the entry format that file specifies.
- **Never** rewrite, condense, or delete the other agent's entries.
- **Claim the files you're touching** in your note before you edit them. If the other agent
  has an open claim on a file, pick a different surface or wait.
- Do not force-push this branch or rewrite shared history.

`HANDOFF.md` also carries the mechanical gotchas (inlined price data, unreadable `data.json`
diffs, `compile_data.py` path and Supabase dependencies, no CI). Read them before editing code.
