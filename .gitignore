# Python bytecode from running compile_data.py / merge_geo.py / apply_gate_and_geo.py
__pycache__/
*.py[cod]

# Local scratch — the real source CSVs for compile_data.py are not in this repo
*.local.json

# Netlify CLI link state, written by `netlify deploy` / the Netlify MCP.
# Machine-local: it only records which site this checkout is linked to.
.netlify/
